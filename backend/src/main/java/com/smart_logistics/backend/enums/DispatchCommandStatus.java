package com.smart_logistics.backend.enums;

public enum DispatchCommandStatus {
    PENDING,
    EXECUTED,
    CANCELLED,
    FAILED,
    SENT,
    ACKNOWLEDGED,
    EXECUTING,
    COMPLETED,
    REJECTED
}
